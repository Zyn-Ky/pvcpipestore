import { Metadata } from "next";

export const metadata: Metadata = {
  title: `Notifikasi`,
};
export default function RootLayoutMobileNotification(props: {
  children: React.ReactNode;
}) {
  return props.children && props.children;
}
